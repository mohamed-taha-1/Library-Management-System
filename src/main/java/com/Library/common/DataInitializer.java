package com.Library.common;import com.Library.models.Book;import com.Library.models.Patron;import com.Library.models.repositories.BookRepository;import com.Library.models.repositories.PatronRepository;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.boot.CommandLineRunner;import org.springframework.stereotype.Component;@Componentpublic class DataInitializer implements CommandLineRunner {  private final BookRepository bookRepository;  private final PatronRepository patronRepository;  public DataInitializer(@Autowired BookRepository bookRepository, @Autowired PatronRepository patronRepository) {    this.bookRepository = bookRepository;    this.patronRepository= patronRepository;  }  @Override  public void run(String... args) {    Book entity = new Book();    entity.setIsbn("978-1-234567-89-0");    entity.setPublicationYear(2023);    entity.setAuthor("author name");    entity.setTitle("Java EE book");    Patron patronEntity= new Patron();    patronEntity.setName("user name");    patronEntity.setContactInformation("my-mail@gmial.com");    // Save the book to the database    bookRepository.save(entity);    patronRepository.save(patronEntity);  }}