# Logging configuration for HTTP requestslogging:  level:    web: debugspring:  h2:    console:      enabled: true  jpa:    show-sql: true  cache:      type: ehcache  security:     user:       name: root       password: root       roles:         - adminserver:  port: 8090# Specify the path of the OpenAPI documentationspringdoc:  api-docs:    path: /api-docs  swagger-ui:    path: /swagger-ui# Set the logging level for the package or class